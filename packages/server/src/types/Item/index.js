import { query } from './query';
import { mutation } from './mutation';
import { subscription } from './subscription';
import typeDefs from './schema.graphql';

export default { query, mutation, subscription, typeDefs };
