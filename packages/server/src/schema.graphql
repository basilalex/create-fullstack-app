scalar Date

type Item {
  id: ID!
  title: String!
  description: String!
  createdAt: Date
  updatedAt: Date
}

type Mutation {
  createItem(title: String, description: String): Item!
}

type Query {
  item(id: ID!): Item
}
