scalar Date

type Item {
  id: Int!
  title: String!
  description: String!
  createdAt: Date
  updatedAt: Date
}

type Mutation {
  createItem(title: String, description: String): Item!
}

type Query {
  item(id: Int!): Item,
  items: [Item]!
}
